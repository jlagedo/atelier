/// <reference types="vite/client" />

// Injected by @electron-forge/plugin-vite at build time. Present (a URL) only in dev;
// replaced/undefined in production builds — guard usage with a truthiness check.
// No import/export in this file: keeps these declarations global (ambient script, not a module).
declare const MAIN_WINDOW_VITE_DEV_SERVER_URL: string;
declare const MAIN_WINDOW_VITE_NAME: string;
