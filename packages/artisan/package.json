{
  "name": "@atelier/artisan",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The artisan agent loop — hosts @anthropic-ai/claude-agent-sdk (Topology A host loop + Topology B in-guest loop). See docs/design.md §8.",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "tsx src/cli.ts",
    "start": "tsx src/cli.ts",
    "start:guest": "tsx src/cli-guest.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.0",
    "tsx": "^4.20.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
